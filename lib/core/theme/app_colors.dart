import 'package:flutter/material.dart';

// Matches the design tokens established in the Figma/Stitch exploration —
// keep this file and that palette in sync if the direction changes.
class AppColors {
  const AppColors._();

  static const Color background = Color(0xFF0B0D0D);
  static const Color surface = Color(0xFF151A19);
  static const Color border = Color(0xFF232B2A);
  static const Color textPrimary = Color(0xFFF4F7F6);
  static const Color textSecondary = Color(0xFF93A19D);
  static const Color accent = Color(0xFFF5A524);
  static const Color accentOn = Color(0xFF1A1200);
  static const Color success = Color(0xFF34D399);
  static const Color danger = Color(0xFFF87171);
}
