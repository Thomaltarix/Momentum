// Placeholder until Phase 5 onboarding lets the user set a real goal (see
// claude/roadmap.md). 5000 matches what was actually asked for when this
// app was scoped — not an arbitrary default.
const int defaultDailyStepGoal = 5000;
